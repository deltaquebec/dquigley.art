---
title: Obscur
description: "Obscuring the camera"
# images are read from /album/obscur/, thumbnails from /thumb/obscur/.
# `dir` defaults to the file name; set it only if the folder differs from the slug.
# `cover` defaults to the first image; set it to pin a specific one.
# optional per-image captions, keyed by file basename (no extension):
# captions:
#   berlin-01: "Alexanderplatz at dusk."
---
